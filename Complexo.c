#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#include "Complexo.h"


TYPED(complexo_t)* criaComplexo() {
    TYPED(complexo_t)* variavel = malloc(sizeof(TYPED(complexo_t));
    return variavel;
}
